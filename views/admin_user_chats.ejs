<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title><%= user.name %> Chats</title>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-dark text-white">
<div class="container mt-5">
    <h1 class="mb-4"><%= user.name %> (<%= user.email %>) Chat History</h1>

    <table class="table table-dark table-striped">
        <thead>
            <tr>
                <th>Role</th>
                <th>Message</th>
            </tr>
        </thead>
        <tbody>
            <% history.forEach(chat => { %>
            <tr>
                <td><%= chat.role %></td>
                <td><%= chat.content %></td>
            </tr>
            <% }) %>
        </tbody>
    </table>

    <form action="/admin/user/<%= user.email %>/delete-chats" method="POST">
        <button type="submit" class="btn btn-danger mt-3" onclick="return confirm('Delete all chats?')">Delete All Chats</button>
    </form>

    <a href="/admin/dashboard" class="btn btn-secondary mt-3">Back to Dashboard</a>
</div>
</body>
</html>
